#include <bits/stdc++.h>
using namespace std;
int main() {
    ios_base::sync_with_stdio;
    cin.tie(0);
    while(1) {
        int a;
        cin >> a;
        
        for(int i = 0; i < a; i++) {
            
        }
        if (a == 0) {
            break;
        }
    } 
    return 0;
}