#include <bits/stdc++.h>
using namespace std;

int main() {
    set<string> key;
    string input;
    
    for(int i = 1; i <= 4; i++) {
        cin >> input;          
        key.insert(s);     
    }

    if (key.size() == 4) {   
        cout << "Yes\n";
    } else {
        cout << "No\n";
    }

    return 0;
}
