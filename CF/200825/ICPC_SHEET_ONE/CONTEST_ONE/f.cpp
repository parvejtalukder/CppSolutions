#include <bits/stdc++.h> 
using namespace std; 
int main() { 
    ios_base::sync_with_stdio(false); 
    cin.tie(0); 
    unsigned int a, b, ans; 
    cin >> a >> b; 
    ans = a ^ b; 
    cout << ans << endl; 
    return 0; 
}