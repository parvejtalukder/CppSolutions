#include <iostream>
using namespace std;
main() {
    int no;
    cin >> no;
    int arr[no];
    for (int i; i < no; i++)
    {
        cin >> arr[i];
    }
    for (int i = 0; no; i++) {
        cout << arr[i];
        cout << endl;
    }
}
