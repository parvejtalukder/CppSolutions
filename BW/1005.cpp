#include <bits/stdc++.h>
using namespace std;
int main() {
    ios_base::sync_with_stdio;
    cin.tie(0);
    double one, two;
    cin >> one >> two;
    cout << fixed << setprecision(5) << "MEDIA = " << (one + two) / 2.00 << endl;
    return 0;
}