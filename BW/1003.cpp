#include <bits/stdc++.h>
using namespace std;
int main() {
    ios_base::sync_with_stdio;
    cin.tie(0);
    long long int one, two;
    cin >> one >> two;
    cout << "SOMA = " << one + two << endl;
    return 0;
}