#include <string.h>
#include <bits/stdc++.h>
using namespace std;

int main() {
    char myName[40];
    cin.getline(myName, 40);
    int size = strlen(myName);
    cout << myName << endl << size << endl;
    int charSize;
    charSize = size;
    return 0;
}