#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio;
    cin.tie(0);
    unsigned int a, b;
    while (cin >> a >> b) { 
        cout << (a ^ b) << "\n"; 
    }
    return 0;
}
